{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/MainServer/MainServer.IS4Host/MainServer.IS4Host.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}